import React from 'react';

const HeroSection = () => {
    return (
        <div>
            <nav>
                <img src="/images/nav-logo.svg" alt="logo" className="scale-90" />
                <img src="/images/menu.svg" alt="logo" className="w-10" />
            </nav>
        </div>
    );
};

export default HeroSection;
